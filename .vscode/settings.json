{
  "clangd.arguments": [
    "--query-driver=**/bin/riscv32-esp-elf-*"
  ],
  "platformio-ide.useDevelopmentPIOCore": true,
  "terminal.integrated.env.osx": {
    "PATH": "${workspaceFolder}/.venv/bin:${env:HOME}/.platformio/penv/bin:${env:PATH}"
  },
  "terminal.integrated.env.linux": {
    "PATH": "${workspaceFolder}/.venv/bin:${env:HOME}/.platformio/penv/bin:${env:PATH}"
  }
}
